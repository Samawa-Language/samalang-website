---
title: then
description: A then Guides Page
---

## Description
The `tres` keyword in Samalang marks the start of the code block after `if`, `elseif`, or `for`.  
It is equivalent to `then` in standard Lua.

## Usage
```bash
lamen condition tres
    -- code
jure_mo
```